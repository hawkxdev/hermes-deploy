# Copy this file to /opt/hermes/data/.env on the target host.
# Populate credentials and identifiers only in the private runtime file.

GATEWAY_ALLOW_ALL_USERS=false
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_USERS=
